
## User-defined code

# --> Message Of The Day (MOTD): Funny Quotes
# Full path of quote file
quote_path="/home/mo/projects/msg_of_the_day/hub.sh"
# 
term=$(basename $(tty))
# Check if MOTD script exists
if [[ -s ${quote_path} ]]; then
	case ${term} in
		0)
			# Display quote only on terminal emulator 0
			. ${quote_path} $COLUMNS
			;;
		tty[1-6])
			# Display quote on virtual console 1-6
			. ${quote_path} 160
			;;
	esac
fi
# <-- Message Of The Day (MOTD): Funny Quotes
